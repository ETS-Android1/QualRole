<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.guicelebrini.qualrole">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_release"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_release"
        android:supportsRtl="true"
        android:theme="@style/Theme.QualRole">
        <activity android:name=".activity.AddAnswerActivity"></activity>
        <activity android:name=".activity.QuestionActivity"
            android:exported="true"
            android:parentActivityName=".activity.MainActivity"/>
        <activity android:name=".activity.AddQuestionActivity"
            android:exported="true"
            android:parentActivityName=".activity.MainActivity"/>
        <activity android:name=".activity.InfosActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>